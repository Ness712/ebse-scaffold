# Internationalisation (i18n)

**[RECOMMANDE]** **react-i18next** | Score GRADE : 4/7

## Setup

```bash
npm install react-i18next i18next i18next-http-backend i18next-browser-languagedetector
```

```ts
// src/i18n.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import HttpBackend from 'i18next-http-backend';
import LanguageDetector from 'i18next-browser-languagedetector';

i18n.use(HttpBackend).use(LanguageDetector).use(initReactI18next).init({
  fallbackLng: 'fr',
  supportedLngs: ['fr', 'en'],
  ns: ['common', 'chat', 'lab', 'mycology'],
  defaultNS: 'common',
  interpolation: { escapeValue: false }, // React echappe deja
  backend: { loadPath: '/locales/{{lng}}/{{ns}}.json' },
});
```

```json
// /public/locales/fr/common.json
{
  "nav": { "home": "Accueil", "lab": "Laboratoire", "chat": "Messagerie" },
  "action": { "save": "Enregistrer", "cancel": "Annuler" }
}
```

```tsx
const { t } = useTranslation('mycology');
<h1>{t('pageTitle')}</h1>
```

| Alternative | Verdict |
|---|---|
| FormatJS/react-intl | Plus strict ICU, mais 1.5M dl/sem vs react-i18next 4M |
| next-intl | Next.js uniquement — pas compatible Vite |

Sources : npm trends — react-i18next 4M/sem #1 (niv. 4), State of JS 2024 — i18next #1 awareness (niv. 4), i18next docs (niv. 3)
